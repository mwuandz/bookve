{
  "rules": {
    "rooms": {
      "$room": {
        "seats": {
          "$seatId": {
            ".read": true,
            ".write": "!data.exists() || data.child('userId').val() === newData.child('userId').val()",
            ".validate": "newData.hasChildren(['userId','name','ts'])"
          }
        },
        "claims": { "$userId": { ".read": true, ".write": true } }
      }
    }
  }
}